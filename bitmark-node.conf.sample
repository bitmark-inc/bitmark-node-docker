port = 9980
datadir = "."
versionURL = "https://s3-ap-northeast-1.amazonaws.com/bitmark-node/version.json"

logging {
    directory = "."
    file = "bitmark-node.log"
    size = 1048576
    count = 10
    levels {
        DEFAULT = "info"
    }
}
